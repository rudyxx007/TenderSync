/** Signal Room design system: reusable workspace pieces emphasize evidence, hierarchy, and quiet action. */
import { Link } from '@tanstack/react-router'; import { AlertCircle, ArrowLeft, FileSearch, Plus } from 'lucide-react';
export function PageHeader({ eyebrow, title, text, action, backTo }: { eyebrow?: string; title: string; text?: string; action?: React.ReactNode; backTo?: string }) { return <div className="mb-7 flex flex-col gap-4 border-b border-white/[.08] pb-6 sm:flex-row sm:items-end sm:justify-between"><div>{backTo && <Link to={backTo} className="mb-4 inline-flex items-center gap-1 text-xs font-bold text-zinc-500 hover:text-emerald-300"><ArrowLeft size={14}/>Back</Link>}{eyebrow && <p className="eyebrow text-emerald-400">{eyebrow}</p>}<h1 className="mt-2 text-2xl font-extrabold tracking-[-.05em] text-white sm:text-3xl">{title}</h1>{text && <p className="mt-2 max-w-2xl text-sm leading-6 text-zinc-500">{text}</p>}</div>{action && <div className="shrink-0">{action}</div>}</div>; }
export function LoadingBlock({ label = 'Reading your workspace' }: { label?: string }) { return <div className="panel grid min-h-[260px] place-items-center"><div className="text-center"><i className="mx-auto block h-5 w-5 animate-spin rounded-full border-2 border-emerald-400 border-t-transparent"/><p className="mt-3 font-mono text-[10px] tracking-[.14em] text-zinc-600">{label.toUpperCase()}</p></div></div>; }
export function EmptyState({ title, text, actionTo = '/dashboard', actionLabel = 'Evaluate an RFP' }: { title: string; text: string; actionTo?: string; actionLabel?: string }) { return <div className="panel grid min-h-[310px] place-items-center p-8 text-center"><div className="max-w-sm"><span className="mx-auto grid h-11 w-11 place-items-center rounded-lg bg-emerald-400/10 text-emerald-300"><FileSearch size={20}/></span><h2 className="mt-5 text-lg font-extrabold tracking-[-.03em] text-zinc-100">{title}</h2><p className="mt-2 text-sm leading-6 text-zinc-500">{text}</p><Link to={actionTo} className="button-signal mt-6"><Plus size={15}/>{actionLabel}</Link></div></div>; }
export function ErrorBlock({ text }: { text: string }) { return <div className="rounded-lg border border-rose-400/20 bg-rose-400/[.07] p-4 text-sm text-rose-200"><div className="flex gap-2"><AlertCircle size={16} className="mt-0.5 shrink-0"/><p>{text}</p></div></div>; }
export const formatDate = (value?: string | null) => value ? new Intl.DateTimeFormat(undefined, { dateStyle: 'medium' }).format(new Date(value)) : '—';
